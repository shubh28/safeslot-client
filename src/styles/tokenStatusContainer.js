import styled from 'styled-components';
import { Container as DefaultContainer } from 'reactstrap';

const TokenStatusContainer = styled(DefaultContainer)`
  border:1px solid black;
`;

export default TokenStatusContainer;